# ── Tender Alchemist — Environment Variables ─────────────────────────────────
# Copy to .env and adjust as needed:
#   cp .env.example .env

# ── Web UI ──
WEBUI_PORT=8000

# ── Ollama (LLM) ──
OLLAMA_PORT=11434

# ── Docling (document conversion) ──
DOCLING_PORT=5001
